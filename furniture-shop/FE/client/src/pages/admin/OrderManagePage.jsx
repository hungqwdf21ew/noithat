import DashboardPage from './DashboardPage';

/** Trang quản lý đơn hàng — dùng chung dashboard admin, tab orders */
const OrderManagePage = () => <DashboardPage initialTab="orders" />;

export default OrderManagePage;
